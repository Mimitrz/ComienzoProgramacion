using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Imprimir en la consola algo
        print("Hola mundo");
       // print(22);
      
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
